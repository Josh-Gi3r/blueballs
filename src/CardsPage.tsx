import { useMemo,useState,type ChangeEvent } from "react";
import { CARD_PROGRAMS,type CardProgram,type CardStatus,type CustodyModel } from "./cards/data";
import "./CardsPage.css";
import "./cards/hero-explorer.css";
type CardsPageProps={onNavigate:(path:string)=>void};type Filter="all"|CustodyModel|"Visa"|"Mastercard"|"Consumer"|"Business"|"legacy";
const FILTERS:{id:Filter;label:string}[]=[{id:"all",label:"All programs"},{id:"Self-custodial",label:"Self-custody"},{id:"Custodial",label:"Custodial"},{id:"Hybrid",label:"Hybrid"},{id:"Visa",label:"Visa"},{id:"Mastercard",label:"Mastercard"},{id:"Consumer",label:"Consumer"},{id:"Business",label:"Business"},{id:"legacy",label:"Legacy"}];
function matchesFilter(card:CardProgram,filter:Filter){if(filter==="all")return card.status!=="legacy";if(filter==="legacy")return card.status==="legacy";if(filter==="Visa"||filter==="Mastercard")return card.network.includes(filter)&&card.status!=="legacy";if(filter==="Consumer"||filter==="Business")return(card.customer===filter||card.customer==="Both")&&card.status!=="legacy";return card.custody===filter&&card.status!=="legacy"}function statusLabel(status:CardStatus){return status==="launching"?"LAUNCHING":status==="legacy"?"LEGACY":"ACTIVE"}
function CardTile({card,selected,onSelect,onOpen}:{card:CardProgram;selected:boolean;onSelect:()=>void;onOpen:()=>void}){return <article className={`cards-tile ${selected?"selected":""} ${card.status==="legacy"?"legacy":""}`}><div className="cards-tile-top"><div><span>{statusLabel(card.status)}</span><h3>{card.name}</h3><small>{card.company}</small></div><button className="cards-compare-check" type="button" onClick={onSelect}>{selected?"✓":"+"}</button></div><div className="cards-model-line"><b>{card.network}</b><span>{card.type}</span><span>{card.custody}</span></div><p>{card.note}</p><div className="cards-token-row">{card.funding.slice(0,4).map(item=><span key={item}>{item}</span>)}{card.funding.length>4&&<span>+{card.funding.length-4}</span>}</div><div className="cards-market-grid"><div><small>SPEND MODEL</small><b>{card.spendModel}</b></div><div><small>MARKET OFFER</small><b>{card.rewards}</b></div></div><div className="cards-stack"><small>KNOWN STACK</small><div>{card.partners.length?card.partners.slice(0,4).map((partner,index)=><span key={partner}>{index>0&&<i>→</i>}{partner}</span>):<span>Not publicly disclosed</span>}</div></div><div className="cards-tile-foot"><span>{card.geography}</span><button type="button" onClick={onOpen}>View model <b>→</b></button></div></article>}
function DetailPanel({card,onClose,onCompare,selected}:{card:CardProgram;onClose:()=>void;onCompare:()=>void;selected:boolean}){return <div className="cards-detail-backdrop" onMouseDown={onClose}><aside className="cards-detail" onMouseDown={e=>e.stopPropagation()}><div className="cards-detail-head"><div><span>CARD MODEL</span><h2>{card.name}</h2><p>{card.company} · {card.network} · {card.type}</p></div><button type="button" onClick={onClose}>×</button></div><div className="cards-detail-summary"><div><small>CUSTODY</small><b>{card.custody}</b></div><div><small>CUSTOMER</small><b>{card.customer}</b></div><div><small>FORM</small><b>{[card.virtual&&"Virtual",card.physical&&"Physical"].filter(Boolean).join(" + ")||"Not disclosed"}</b></div></div><section><span>HOW THE MODEL WORKS</span><h3>{card.model}</h3><p>{card.note}</p><div className="cards-flow"><b>{card.wallets}</b><i>→</i><b>{card.spendModel}</b><i>→</i><b>{card.network}</b><i>→</i><b>Merchant</b></div></section><section><span>MONEY</span><div className="cards-detail-list"><div><small>STABLECOINS / FUNDING</small><b>{card.funding.join(" · ")}</b></div><div><small>CHAINS</small><b>{card.chains.join(" · ")}</b></div><div><small>FEES / FX</small><b>{card.fees}</b></div><div><small>REWARDS / PERKS</small><b>{card.rewards}</b></div></div></section><section><span>KNOWN INFRASTRUCTURE</span><div className="cards-partner-chain">{card.partners.map((partner,index)=><span key={partner}>{index>0&&<i>→</i>}<b>{partner}</b></span>)}</div><p className="cards-disclosure">Only relationships supported by the research are shown. Missing issuer, processor, settlement or compliance relationships remain not publicly disclosed.</p></section><section><span>AVAILABILITY</span><p>{card.geography}</p></section><div className="cards-detail-actions"><button type="button" onClick={onCompare}>{selected?"Remove from comparison":"Add to comparison"}</button><button type="button" className="secondary" onClick={onClose}>Back to directory</button></div></aside></div>}
const COMPARE_ROWS:Array<[string,(card:CardProgram)=>string]>=[["Model",c=>c.model],["Card type",c=>`${c.network} · ${c.type}`],["Custody",c=>c.custody],["Funding",c=>c.funding.join(" · ")],["Chains",c=>c.chains.join(" · ")],["Spend model",c=>c.spendModel],["Known stack",c=>c.partners.join(" → ")||"Not publicly disclosed"],["Market offer",c=>c.rewards],["Fees / FX",c=>c.fees],["Geography",c=>c.geography],["Customer",c=>c.customer]];
function CompareTray({cards,onRemove,onClear}:{cards:CardProgram[];onRemove:(id:string)=>void;onClear:()=>void}){const[open,setOpen]=useState(false);if(!cards.length)return null;return <div className="cards-compare-tray"><div className="cards-compare-bar"><div><span>COMPARE ARCHITECTURES</span><strong>{cards.map(c=>c.name).join(" · ")}</strong></div><div><button type="button" className="secondary" onClick={onClear}>Clear</button><button type="button" onClick={()=>setOpen(!open)}>{open?"Close comparison":`Compare ${cards.length}`}</button></div></div>{open&&<div className="cards-compare-table-wrap"><table><thead><tr><th>BUILD DECISION</th>{cards.map(c=><th key={c.id}>{c.name}<button onClick={()=>onRemove(c.id)}>×</button></th>)}</tr></thead><tbody>{COMPARE_ROWS.map(([label,get])=><tr key={label}><td>{label}</td>{cards.map(c=><td key={c.id}>{get(c)}</td>)}</tr>)}</tbody></table></div>}</div>}
const HERO_MODELS=[{id:"wallet",eyebrow:"MODEL 01",title:"Spend from the wallet",copy:"The customer keeps the stablecoin until the card needs it.",flow:["USDC wallet","JIT authorisation","Card issuer","Network"],examples:["MetaMask Card","Solflare Card","MiniPay Card"],filter:"Self-custodial" as Filter},{id:"account",eyebrow:"MODEL 02",title:"Spend from an account",copy:"Stablecoins fund a custodial balance that behaves more like a neobank account.",flow:["USDC / USDT","Account balance","Card programme","Network"],examples:["KAST Card","OKX Card","Crypto.com Visa Card"],filter:"Custodial" as Filter},{id:"credit",eyebrow:"MODEL 03",title:"Borrow against the assets",copy:"The asset backs spending power instead of being sold before every purchase.",flow:["Collateral","Credit line","Authorisation","Network"],examples:["Nexo Card","Avalanche Card","Exa Visa Signature"],filter:"all" as Filter}];
function HeroExplorer({onExplore,onOpen}:{onExplore:(filter:Filter,query?:string)=>void;onOpen:(name:string)=>void}){const[active,setActive]=useState(0);const model=HERO_MODELS[active];return <div className="cards-hero-explorer"><div className="cards-hero-explorer-top"><span>THREE WAYS TO BUILD THE SPEND</span><small>Choose a model to see the architecture.</small></div><div className="cards-hero-model-tabs">{HERO_MODELS.map((item,index)=><button type="button" key={item.id} className={index===active?"active":""} onClick={()=>setActive(index)}><span>0{index+1}</span><b>{item.title}</b></button>)}</div><div className="cards-hero-model-stage"><div className="cards-hero-model-copy"><span>{model.eyebrow}</span><h3>{model.title}</h3><p>{model.copy}</p></div><div className="cards-hero-flow">{model.flow.map((step,index)=><span key={step}><b>{step}</b>{index<model.flow.length-1&&<i>→</i>}</span>)}</div><div className="cards-hero-examples"><small>REAL PROGRAMS USING THIS KIND OF MODEL</small><div>{model.examples.map(name=><button type="button" key={name} onClick={()=>onOpen(name)}>{name}<span>↗</span></button>)}</div></div><button type="button" className="cards-hero-explore-model" onClick={()=>onExplore(model.filter,model.id==="credit"?"credit collateral":"")}>Explore similar programs <span>→</span></button></div></div>}
export default function CardsPage({onNavigate}:CardsPageProps){const[filter,setFilter]=useState<Filter>("all"),[query,setQuery]=useState(""),[selected,setSelected]=useState<string[]>([]),[detail,setDetail]=useState<CardProgram|null>(null);const filtered=useMemo(()=>{const q=query.trim().toLowerCase();return CARD_PROGRAMS.filter(c=>matchesFilter(c,filter)).filter(c=>!q||[c.name,c.company,c.network,c.type,c.custody,c.model,c.spendModel,c.rewards,c.geography,...c.funding,...c.chains,...c.partners].join(" ").toLowerCase().includes(q))},[filter,query]);const compared=selected.map(id=>CARD_PROGRAMS.find(c=>c.id===id)).filter((c):c is CardProgram=>Boolean(c));const toggle=(id:string)=>setSelected(cur=>cur.includes(id)?cur.filter(x=>x!==id):cur.length<4?[...cur,id]:cur);const explore=(nextFilter:Filter,nextQuery="")=>{setFilter(nextFilter);setQuery(nextQuery);setTimeout(()=>document.getElementById("cards-directory")?.scrollIntoView({behavior:"smooth"}),0)};const openByName=(name:string)=>{const card=CARD_PROGRAMS.find(c=>c.name===name);if(card)setDetail(card)};return <div className="cards-page"><section className="cards-hero"><div className="cards-hero-copy"><div className="cards-eyebrow">CARD MODELS</div><h1><span>See how card products</span><span>are being built.</span></h1><p>Explore stablecoin and crypto-enabled card programs by funding model, custody, network, geography, fees, rewards and the providers behind them. Built for teams designing neobanks and financial products.</p><div className="cards-hero-actions"><button onClick={()=>explore("all")}>Explore 38 current programs</button><button className="secondary" onClick={()=>onNavigate("/ecosystem")}>Find infrastructure</button></div><div className="cards-hero-proof"><div><span>41</span><b>public programs researched</b></div><div><span>38</span><b>active or launching</b></div><div><span>3</span><b>legacy models retained</b></div></div></div><HeroExplorer onExplore={explore} onOpen={openByName}/></section><section id="cards-directory" className="cards-directory"><div className="cards-directory-head"><div><span>CARD MODEL DIRECTORY</span><h2>Study the market before you design the stack.</h2><p>Compare what users are being offered and how the card appears to work underneath. This is not a ranking.</p></div><label className="cards-search"><span>SEARCH</span><input value={query} onChange={(e:ChangeEvent<HTMLInputElement>)=>setQuery(e.target.value)} placeholder="Card, stablecoin, partner, model or region…"/></label></div><div className="cards-filter-row">{FILTERS.map(item=><button type="button" className={filter===item.id?"active":""} key={item.id} onClick={()=>setFilter(item.id)}><b>{item.label}</b><span>{CARD_PROGRAMS.filter(c=>matchesFilter(c,item.id)).length}</span></button>)}</div><div className="cards-results-line"><span>{filtered.length} {filtered.length===1?"program":"programs"}</span><span>Research snapshot · 12 Aug 2026 · Unknown fields are left undisclosed</span></div><div className="cards-grid">{filtered.map(c=><CardTile key={c.id} card={c} selected={selected.includes(c.id)} onSelect={()=>toggle(c.id)} onOpen={()=>setDetail(c)}/>)}</div>{!filtered.length&&<div className="cards-empty">No card programs match this search.</div>}</section><section className="cards-market-note"><div><span>READ THE STACK, NOT JUST THE CARD</span><h2>The same customer proposition can sit on very different infrastructure.</h2><p>A card brand may use a self-custodial wallet, a program manager, a separate BIN sponsor, another processor and a network such as Visa or Mastercard. Blueballs keeps those relationships separate so builders can see the actual model where it is publicly known.</p></div><div className="cards-market-stack"><span>BRAND</span><i>→</i><span>WALLET / ACCOUNT</span><i>→</i><span>PROGRAM / ISSUER</span><i>→</i><span>NETWORK</span><i>→</i><span>MERCHANT</span></div></section><section className="cards-bottom"><div><span>FROM MARKET MODEL TO YOUR STACK</span><h2>Found a model you want to understand?</h2><p>Use the Providers directory to explore issuing, banking, custody, compliance, FX, stablecoin and payment infrastructure that could sit behind your own product.</p></div><div><button onClick={()=>onNavigate("/ecosystem")}>Browse providers</button><button className="secondary" onClick={()=>onNavigate("/developers")}>See Blueballs APIs</button></div></section><div className="cards-disclaimer"><span>ABOUT THIS DIRECTORY</span><p>Card programs, fees, rewards, eligibility and provider relationships change. The directory keeps unknown relationships as not publicly disclosed rather than inferring them. Research snapshot: 12 Aug 2026.</p></div>{detail&&<DetailPanel card={detail} onClose={()=>setDetail(null)} onCompare={()=>toggle(detail.id)} selected={selected.includes(detail.id)}/>}<CompareTray cards={compared} onRemove={toggle} onClear={()=>setSelected([])}/></div>}
